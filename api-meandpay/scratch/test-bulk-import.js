import fetch from "node-fetch";

async function test() {
    try {
        // Need a valid token or bypass auth? Actually we can just check what the server says if we send an invalid request.
        // Wait, auth is required. Is there a way to call it bypassing auth, or maybe check the logs?
    } catch(e) {}
}
test();
